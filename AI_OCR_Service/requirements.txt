# FastAPI & Server
fastapi
uvicorn[standard]
python-multipart

# JSON Serialization
orjson>=3.9.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Caching
redis==4.6.0
cachetools>=5.3.0

# Async Logging
aiologger>=0.7.0
loguru>=0.7.2

# HTTP Client
httpx[http2]>=0.25.0
aiohttp>=3.9.0

# Configuration
python-dotenv

# Google Cloud
google-cloud-vision
google-genai

# Elasticsearch
elasticsearch[async]>=8.0.0,<9.0.0

# Image Processing - Pinned for compatibility with PaddleOCR
Pillow==10.4.0
numpy==1.26.4
opencv-python==4.8.1.78

# PaddleOCR - Standard version with compatible dependencies
# NOTE: paddlepaddle 2.6.2 requires protobuf<=3.20.2
# This conflicts with some Google Cloud and OpenTelemetry packages
# but core OCR functionality works correctly
paddlepaddle==2.6.2
paddleocr>=2.7.0

# ML/AI Libraries
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# LangChain
langchain>=0.1.0
langchain-core>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.0.5
langchain-groq>=0.1.0
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1
langgraph>=0.0.20
langsmith>=0.0.50

# Vector Stores
chromadb>=0.4.0
faiss-cpu>=1.7.0

# Utilities
structlog>=23.0.0
psutil
safetensors

# NOTE: azure-monitor-opentelemetry and langfuse have dependency conflicts
# with paddlepaddle's protobuf requirement. These are optional monitoring
# packages. To use them, you would need to:
# 1. Remove paddlepaddle/paddleocr and use Google Cloud Vision only, OR
# 2. Create a separate virtual environment for PaddleOCR
# azure-monitor-opentelemetry
# langfuse

# Protobuf - Pinned for PaddleOCR compatibility (<=3.20.2)
# This conflicts with google-api-core>=2.30.0 which requires protobuf>=4.25.8
# The application works but pip check will show warnings
protobuf==3.20.2

# Additional dependencies for compatibility
scipy>=1.11.0,<1.14.0
numba>=0.58.0